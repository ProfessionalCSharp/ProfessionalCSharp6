<Page
    x:Class="LayoutSamples.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LayoutSamples"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <Button Content="Variable Sized Wrap Grid" Click="{x:Bind OnGotoVariableSizedWrapGrid}" />
            <Button Content="Relative Panel" Click="{x:Bind OnRelativePanel}" />
            <Button Content="Adaptive Relative Panel" Click="{x:Bind OnAdaptiveRelativePanel}" />
            <Button Content="Delay Loading" Click="{x:Bind OnDelayLoading}" />
        </StackPanel>

    </Grid>
</Page>
